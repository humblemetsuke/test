name: CI test to see how it works

on:
  push:
    branches: [ main ]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: say hello
        run: echo "Hello world mofo!"

      - name: Intentional failure
        run: exit 0


